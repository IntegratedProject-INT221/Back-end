version: "3"
services:
  backend:
    build: .
    image: backend
    ports:
      - "3000:3000"
    container_name: backend
    environment:
      - MYSQL_DATABASE=ProjectINT221
      - MYSQL_USER=admin
      - MYSQL_ROOT_PASSWORD=adminINT221
    volumes:
      - ~/backend_data:/images
networks:
  default:
    external:
      name: project221